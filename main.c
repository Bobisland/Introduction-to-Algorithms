#include "sort.h"
#include <stdio.h>
#include "1234 to 1 2 3 4.h"
int main()
{
//    int arr[5] = {};
//    for(int times = 0;times < 5;times++)
//    {
//        scanf("%d",&arr[times]);
//    }
//    int n = sizeof(arr) / sizeof(arr[0]);
//      sort(arr,n);

    int num1;
    scanf("%d",&num1);
    num_sort(num1);

    return 0;

}